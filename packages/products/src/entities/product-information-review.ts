/**
 * Object used to aggregate both responses from the different API calls
 * The idea is to have all centralized in one single object and also distiguish between one response and the other.
 */

export interface IProductInformationReview{
    [key: string]: any;
}